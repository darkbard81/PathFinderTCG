# `*.test.ts` 작성 지침

## 작성한다

* 게임 규칙, 상태 변화, 계산 로직
* 비동기 처리와 오류 처리
* 이벤트·타이머·구독의 해제
* 여러 모듈 간 연결
* 실제 발생한 버그의 재발 방지
* 실패 시 사용자 영향이 큰 기능

## 작성하지 않는다

* getter, setter, 단순 전달 함수
* 타입 검사로 충분한 코드
* 프레임워크 동작 자체를 확인하는 테스트
* 구현 코드를 그대로 복사한 테스트
* 커버리지 숫자만 높이기 위한 테스트
* 상위 통합 테스트에서 이미 검증되는 동작

## 원칙

1. 내부 구현보다 외부에서 보이는 결과를 검증한다.
2. Mock보다 실제 프로젝트 객체를 우선한다.
3. 호출 횟수는 중복 실행이 실제 문제일 때만 검증한다.
4. 단순 이벤트 매핑은 가능하면 통합 테스트에서 검증한다.
5. 테스트 이름은 사용자 문제나 기대 동작을 표현한다.

## 작성 전 확인

다음 질문에 명확히 답할 수 있어야 한다.

> 이 테스트가 실패하면 어떤 실제 문제를 발견한 것인가?

답이 불분명하면 테스트를 작성하지 않는다.
